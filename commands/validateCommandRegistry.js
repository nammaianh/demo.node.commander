'use strict';

const joi = require('joi');

/**
 * A command registry object must satisfy these requirements:
 * 1. ...
 * 2. ...
 * 3. ...
 *
 * @param {Object} registry A command definition object
 */
module.exports = function validateCommandRegistry (registry) {
  // TODO: Implementation
};
